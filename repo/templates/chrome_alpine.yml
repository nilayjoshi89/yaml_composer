### SAMPLE ARGUMENTS
# (none — container name is the only argument)
### FUNCTION CALL
# X-REF-FUNCT-TEMPLATE-CHROME-ALPINE("chrome-alpine"):
###

X-REF-FUNCT-TEMPLATE-CHROME-ALPINE:
  #INHERIT FROM BASE TEMPLATE
  X-REF-BASE-TEMPLATE:
  image: X-REF-IMAGE-CHROME-ALPINE
  container_name: "{X-ARG-1}"
  command:
    - --no-sandbox
    - --disable-gpu
    - --disable-dev-shm-usage
    - --remote-debugging-address=0.0.0.0
    - --remote-debugging-port=9222
    - --hide-scrollbars
